# <span class="color-purple">Analyzing The subject:</span>

...loading

---

# <span class="color-cyan">Perfect maze generator Algorithm</span>

this takes the data from the parsed config files and use it to create the maze ==<span class="color-red">how the a_maze_ing.py will interact with the config file ?</span>==

---

# <span class="color-purple">Makefile</span>

> <span class="color-green">Make file in python is used like automation system or shortcuts platform to build the project:</span>

- Install Dependencies.
- Create/Remove/Initial the virtual Environment.
- Generate the start point files (map, data, assets)
- Install Packages
- Or even format the code with a specific code standard like `flake8`

### <span class="color-cyan">How to use it</span>

```python
make run
```

**<span class="color-pink">To install Dependencies</span>**

```python
make install
make run-venv
```

**<span class="color-pink">Clean Files</span>**

```css
make clean
```

**<span class="color-pink">remove everything (venv)</span>**

```css
make fclean
```

# <span class="color-purple">Make For This Project</span>

### <span class="color-cyan">1) `make install`</span>

> <span class="color-green">_Should prepare the environment, so your code can run_</span>

**<span class="color-pink">What should do exactly</span>**

- install dependencies (flake8, mypy, maybe colorama, etc.)
- usually using: `pip install -r requirements`

### <span class="color-cyan">2) `make run`</span>

> <span class="color-green">_Run your program **Normally**_</span>

- Execute main script with `Python`.
- With a default/specific config file.

### <span class="color-cyan">3) `make Debug`</span>

> <span class="color-green">_Run your program but inside a debugger_</span>

**<span class="color-pink">Using Python built-in debugger (`pdb`)</span>** _Example_

```css
python3 -m pdb a_maze_ing.py config.txt
```

Then you can step through code (step...step), inspect variables, etc.

### <span class="color-cyan">4) `make clean`</span>

> <span class="color-green">Remove All garbage/cache files generated by python tools</span>

**<span class="color-pink">What should delete</span>**

- `__pycache__/`
- `*.pyc`
- `.mypy_cache/`
- `.pytest_cache/` (if you use pytest)
- `.ruff_cache/` (if you ever use ruff)

**_Note:_** clean = "remove temporary python-generated junk files"

### <span class="color-cyan">5) `make lint`</span>

==<span class="color-red">The subject explicitly demands this command:</span>==

> <span class="color-green">Used to check the code style and format/show rules to be followed.</span>

It must run:

```js
flake8 .
mypy . --warn-return-any --warn-unused-ignores --ignore-missing-imports \
       --disallow-untyped-defs --check-untyped-defs=
```

### <span class="color-cyan">6) `make lint-strict`</span>

Runs:

```js
flake8 .
mypy . --strict
```

_lint-strict = "hard mode mypy check"_

---

# <span class="color-purple">Config file</span>

The